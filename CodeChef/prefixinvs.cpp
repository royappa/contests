//https://www.codechef.com/LTIME48/problems/TELEPORT

#include <bits/stdc++.h>

using namespace std;

typedef long long LL;


int main()
{
	int Q, R;
	cin >> Q >> R;
	while (Q--) {
		string op;
		cin >> op;
		if (op == "+") {
			int x, y;
			v.push_back(make_pair(x, y));
		}
	}
}

