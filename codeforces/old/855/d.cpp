#include <bits/stdc++.h>

using namespace std;
typedef long long LL;

int main()
{
	int n;
	cin >> n;
	int p[n], t[n];
	for (int i = 0; i < n; i++) {
		scanf("%d%d", &p[i], &t[i]);
	}
	int q;
	cin >> q;

	for (int i = 0; i < q; i++) {
		int qt, u, v;
		scanf("%d%d%d", &qt, &u, &v);
	}


	exit(0);
}
