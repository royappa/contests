#include <bits/stdc++.h>

using namespace std;
typedef long long LL;

int main()
{
	LL n, k, A, B;
	cin >> n >> k >> A >> B;
	multi_set<LL> v;
	for (int i = 0; i < k; i++) {
		cin >> x;
		v.insert(x);
	}

	cout << 0 << endl;
	exit(0);
}

