#include <bits/stdc++.h>

using namespace std;

typedef long long LL;
typedef pair<LL,LL> pLL;

// Algorithm

main()
{
	int n, k;
	cin >> n >> k;

	vector<int> v(n);
	for (int i = 0; i < n; i++) {
		cin >> v[i];
	}

	exit(0);
}
